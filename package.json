{
  "name": "mapx-demo-embed",
  "version": "1.0.0",
  "description": "Proof of concept embedding MapX disaster risk reduction maps with Mangrove UI controls.",
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@vitest/coverage-v8": "^4.1.5",
    "jsdom": "^29.1.1",
    "vite": "^8.0.0",
    "vitest": "^4.1.5"
  }
}
